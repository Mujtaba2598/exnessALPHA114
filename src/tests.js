const test=require('node:test');const assert=require('node:assert');const ai=require('./ai/signal-engine');const I=require('./indicators');test('indicators return finite values',()=>{const cs=[];let p=100;for(let i=0;i<120;i++){p+=Math.sin(i/7)*0.8+0.2;cs.push({o:p-1,h:p+1,l:p-2,c:p})}assert.ok(Number.isFinite(I.ema(cs.map(x=>x.c),20)));assert.ok(Number.isFinite(I.rsi(cs.map(x=>x.c))));assert.ok(Number.isFinite(I.atr(cs)));assert.ok(ai.evaluate(cs))})