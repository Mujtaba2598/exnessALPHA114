function protectiveLevels({entry,stop,target,minRewardRisk}){const risk=Math.abs(entry-stop),reward=Math.abs(target-entry);return {ok:risk>0&&reward/risk>=minRewardRisk,entry,stop,target,risk,rewardRR:reward/risk}}
function sizePosition({equity,cash,entry,stop,settings}){const riskCash=equity*(settings.riskPercent/100);const perUnit=Math.abs(entry-stop);if(riskCash<=0||perUnit<=0)return {qty:0};let qty=riskCash/perUnit;const maxNotional=equity*((settings.maxPositionPercent||10)/100);qty=Math.min(qty,maxNotional/Math.max(entry,1));const reserve=equity*(settings.cashReservePercent/100);const cashAvailable=Math.max(0,cash-reserve);qty=Math.min(qty,cashAvailable/Math.max(entry,1));return {qty,notional:qty*entry,riskCash}}
function trailStop({currentStop,highWater,price,atrValue,entry}){if(!Number.isFinite(atrValue))return {stop:currentStop,highWater};if(price>highWater)highWater=price;return {highWater,stop:Math.max(currentStop,Math.min(price-1.4*atrValue,entry))}}
module.exports={protectiveLevels,sizePosition,trailStop};
