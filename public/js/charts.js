(()=>{function draw(canvas,values){const dpr=devicePixelRatio||1,w=canvas.clientWidth||600,h=canvas.height||260;canvas.width=w*dpr;canvas.height=h*dpr;const c=canvas.getContext('2d');c.scale(dpr,dpr);c.clearRect(0,0,w,h);if(!values?.length)return;const min=Math.min(...values),max=Math.max(...values),span=max-min||1;c.strokeStyle='rgba(255,255,255,.06)';for(let i=0;i<4;i++){const y=20+i*(h-40)/3;c.beginPath();c.moveTo(12,y);c.lineTo(w-12,y);c.stroke()}c.strokeStyle=getComputedStyle(document.documentElement).getPropertyValue('--accent');c.lineWidth=2;c.beginPath();values.forEach((v,i)=>{const x=14+i*(w-28)/Math.max(1,values.length-1);const y=h-18-(v-min)/span*(h-36);i?c.lineTo(x,y):c.moveTo(x,y)});c.stroke()}window.Charts={draw}})();