const sma=(a,n)=>a.length<n?null:a.slice(-n).reduce((s,x)=>s+x,0)/n;function ema(a,n){if(a.length<n)return null;const k=2/(n+1);let e=a.slice(0,n).reduce((s,x)=>s+x,0)/n;for(let i=n;i<a.length;i++)e=a[i]*k+e*(1-k);return e}function rsi(a,n=14){if(a.length<n+1)return null;let g=0,l=0;for(let i=a.length-n;i<a.length;i++){const d=a[i]-a[i-1];if(d>=0)g+=d;else l-=d}if(l===0)return 100;return 100-(100/(1+(g/n)/(l/n)))}function tr(c,i){if(i===0)return c[i].h-c[i].l;return Math.max(c[i].h-c[i].l,Math.abs(c[i].h-c[i-1].c),Math.abs(c[i].l-c[i-1].c))}function atr(c,n=14){if(c.length<n)return null;let v=0;for(let i=c.length-n;i<c.length;i++)v+=tr(c,i);return v/n}function macd(a,fast=12,slow=26,sig=9){if(a.length<slow+sig)return null;const line=[];for(let i=slow;i<=a.length;i++){const w=a.slice(0,i);line.push(ema(w,fast)-ema(w,slow))}const m=ema(line,sig);return {line:line[line.length-1],signal:m,hist:line[line.length-1]-m}}
module.exports={sma,ema,rsi,atr,macd};
